services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: lst
      POSTGRES_PASSWORD: lst
      POSTGRES_DB: lst_kuep
    ports:
      - "5432:5432"
    volumes:
      - lst_pg_data:/var/lib/postgresql/data

volumes:
  lst_pg_data:
